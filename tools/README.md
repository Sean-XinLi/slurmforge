# Tools

Repository maintenance scripts live here.

Rules:

- Tools must be safe to run from the repository root.
- Tools used by tests or CI must support `--check`.
- Tools must not require runtime dependencies outside project dev dependencies.
- Tools must not write outside the repository.
